# Backend Firebase Admin
# Recommended: set FIREBASE_SERVICE_ACCOUNT_PATH to the downloaded service account JSON file
# Example: FIREBASE_SERVICE_ACCOUNT_PATH=./secrets/serviceAccountKey.json
FIREBASE_SERVICE_ACCOUNT_PATH=

# Alternative (not recommended): inline JSON (must be valid JSON)
FIREBASE_SERVICE_ACCOUNT_JSON=

# Optional: override port
PORT=3001

# File uploads
# Directory (relative to backend/) where uploaded files are stored.
# The backend will serve them at http://localhost:<PORT>/uploads/...
UPLOADS_DIR=uploads

# Vehicles storage
# Default: Firestore (falls back to local file if Firestore isn't available)
# Set to 'file' if you want to force local JSON persistence.
VEHICLES_PERSISTENCE=firestore

# Firestore collection name for user-posted vehicles
VEHICLES_COLLECTION=vehicles
